// Operadores
// Classificação:
// Binários
// Unários
// Ternários


//Binários:
// um operador (+) e dois operandos (3)
let binario = 3 + 3;
binario = 7 - b;
// Continua sento binário. Pois é de dois em dois.
binario = 7 - b + 3 / c; 

//Unários:
// um operador (a++)
let unario = a++;

//Unários:
// operador (?) trabalha em x, a e b. Opera em três partes.
let ternario = x ? a:b;