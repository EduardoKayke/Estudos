// String, number, null, boolean, undefined, symbol.

// String é um tipo de dado primitivo.
const nome = 'Eduardo';
const nome1 = "Eduardo";
const nome2 = `Eduardo`;
console.log(typeof(nome));

// Number é um tipo de dado primitivo.
const numero = 27; // Inteiro.
const numero1 = 56.65; // Flutuante.
console.log(typeof(numero));

// Undefined é um tipo de dado primitivo. Não aponta para lugar algum da memória.
let nomeAluno; // Declarar a variável sem atribuir valor.
// o Null é para desconfigurar um valor da variável.
const sobrenomeAluno = null; // Nulo -> Não aponta para lugar algum da memória.
// Exemplo - Criamos um site, o usuário escolhe uma cor, enquanto não escolher o valor é nulo.
// A diferença entre Null e Undefined é que em uma escolhemos não ter valor, já a undefined é o JavaScript.

// Bolean é um tipo de dado primitivo. (True, False, Lógico)
const boolean = true;
const boolean1 = false;

// Tipo d